# Bahor AI Migration Guide

## Migration from Lovable Cloud to Personal Supabase

This guide helps you complete the migration of Bahor AI to your personal Supabase project.

---

## ✅ Completed Steps

- [x] Updated `.env` with new Supabase credentials
- [x] Pushed changes to GitHub repository

---

## 🔧 Manual Steps Required

### Step 1: Run the SQL Migration

1. Go to your Supabase Dashboard: https://supabase.com/dashboard/project/tqbnmozkfvcqekfkqknx/sql/new
2. Open the file `bahor-ai-complete-migration.sql` (attached below or download from the docs)
3. Copy the entire SQL content and paste it into the SQL Editor
4. Click **Run** to execute

The SQL will create:
- 40+ tables (profiles, chat_threads, chat_messages, usage_counters, etc.)
- All RLS (Row Level Security) policies
- Functions and triggers (including handle_new_user)
- Storage buckets

### Step 2: Add Edge Function Secrets

Go to: **Settings → Edge Functions → Secrets** in your Supabase Dashboard

Add the following secrets:

| Secret Name | Description | Required |
|-------------|-------------|----------|
| `SUPABASE_URL` | `https://tqbnmozkfvcqekfkqknx.supabase.co` | ✅ Yes |
| `SUPABASE_SERVICE_ROLE_KEY` | Your service role key | ✅ Yes |
| `SUPABASE_ANON_KEY` | Your anon/publishable key | ✅ Yes |
| `DEEPSEEK_API_KEY` | DeepSeek AI API key for chat | ✅ Yes |
| `GOOGLE_SEARCH_API_KEY` | Google Custom Search API | Optional |
| `GOOGLE_CX` | Google Custom Search Engine ID | Optional |
| `FIREWORKS_API_KEY` | Fireworks AI for image generation | Optional |
| `PIAPI_API_KEY` | PiAPI for fast image generation | Optional |
| `GROQ_API_KEY` | Groq for fast inference | Optional |
| `RESEND_API_KEY` | Email sending via Resend | Optional |
| `REPLICATE_API_TOKEN` | Replicate AI models | Optional |
| `RUNPOD_API_KEY` | RunPod video generation | Optional |
| `ATMOS_CONSUMER_ID` | ATMOS payment merchant ID | For payments |
| `ATMOS_CONSUMER_SECRET` | ATMOS payment secret | For payments |
| `ATMOS_STORE_ID` | ATMOS store identifier | For payments |
| `ATMOS_API_BASE` | ATMOS API base URL | For payments |
| `FIXIE_URL` | Fixie static IP proxy URL | For payments |
| `DEV_UNLIMITED_EMAILS` | Comma-separated dev emails | Optional |
| `ADMIN_EMAILS` | Comma-separated admin emails | Optional |

### Step 3: Deploy Edge Functions

Run this command in your project directory:

```bash
supabase functions deploy --project-ref tqbnmozkfvcqekfkqknx
```

Or deploy individual functions:

```bash
supabase functions deploy chat --project-ref tqbnmozkfvcqekfkqknx
supabase functions deploy extract-attachment-text --project-ref tqbnmozkfvcqekfkqknx
supabase functions deploy atmos-create-transaction --project-ref tqbnmozkfvcqekfkqknx
```

### Step 4: Configure Google OAuth (Optional)

If you want Google Sign-In:

1. Go to **Authentication → Providers → Google** in Supabase Dashboard
2. Add your Google OAuth Client ID and Secret
3. Set the redirect URL to your app's callback URL

---

## 📱 Mobile App Configuration

The mobile app has been configured to use your new Supabase project. The credentials are:

```
EXPO_PUBLIC_SUPABASE_URL=https://tqbnmozkfvcqekfkqknx.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_K0FhhfSxIt3m1KvoCqVfQA_by-P2aN5
```

---

## 🔍 Validation Checklist

After completing the migration:

- [ ] Test user registration/login
- [ ] Verify chat messages are saved
- [ ] Check usage counters are tracking
- [ ] Test file attachments
- [ ] Verify payment flow (if applicable)

---

## 📞 Support

If you encounter issues, check:
1. Supabase Dashboard logs
2. Edge Function logs
3. Browser console for frontend errors

---

*Migration guide generated by Manus AI*
